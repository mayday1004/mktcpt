{
  "name": "buyads-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev-config": "node scripts/write-dev-config.js",
    "preview": "node build.js && npx serve dist -p 8080",
    "as:deploy": "node scripts/deploy-apps-script.mjs",
    "as:rotate-url": "node scripts/rotate-exec-url.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "javascript-obfuscator": "^4.1.1"
  }
}
