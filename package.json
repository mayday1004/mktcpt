{
  "name": "buyads-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev-config": "node scripts/write-dev-config.js",
    "preview": "node build.js && npx serve dist -p 8080"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "javascript-obfuscator": "^4.1.1"
  }
}
