# 廣告投放管理系統 - 規格文件

## 1. 產品列表

### APP 類產品（每日攤提可 ±30%）
| 產品名稱 | 產品代碼 | 備註 |
|---|---|---|
| 愛威奶 | `AV9` | |
| 愛威奶破圈 | `av9_poquan` | 獨立產品，規則與愛威奶同 |
| 健康 | `JK` | |
| 健康破圈 | `jk_poquan` | 獨立產品，規則與健康同 |
| 黃油圈 | `HYC` | |

> 破圈是獨立產品、不與母產品共用預算、不套用特別規則。唯一的特別之處在於「匯入舊 xlsx」時的來源拆分 — 詳見 §9.1。

### 小島型產品（每日攤提只能 ±0.5%）
| 產品名稱 | 產品代碼 |
|---|---|
| 破解吧 | `PJ8` |
| 汁婦寶 | `ZFB` |
| 萬精游 | `OJI` |
| 磨欲爽 | `MYS` |
| 色軟庫 | `XRK` |
| 熊貓巴士 | `BS` |

---

## 2. 核心計算規則

### 2.1 每日攤提
- **廣告攤提天數 = 手動填寫**（不要用 `結束日 - 開始日` 推算）
- **每日攤提 = 廣告金額（台幣） / 攤提天數**
- **費用計算區間：開始日當天開始算，結束日當天不算錢**
- 範例：9 萬人民幣廣告 / 30 天 = 每日 3,000 人民幣攤提（再依匯率換台幣）

### 2.2 共購分權
一個廣告可由多個產品共同採買，每個產品分配一個 % 權重，當天攤提費用依權重分攤。

範例：9 萬廣告 30 天，每日 3,000 攤提
- A 50% → 每日花 1,500
- B 30% → 每日花 900
- C 10% → 每日花 300
- D 10% → 每日花 300

### 2.3 獨立採買（同廣告代碼、各自買）
部分廣告雖是同一個廣告代碼/名稱，但由不同產品**各自獨立採買**，各自佔 100% 權重。

範例：廣告代碼 70「成人色導航」
- AV9 採買一份，AV9 權重 100%
- JK 採買一份，JK 權重 100%

資料模型要能區分「共購」與「獨立採買」。

### 2.4 權重單位
- 最小調整單位 **1%**
- 每個廣告所有產品權重加總 = 100%（共購）或各自 100%（獨立採買）

---

## 3. 預算規則

### 3.1 月結容差
- **允許少花上限：2 萬台幣**
- **超花絕對上限：1 萬台幣**（絕對不能超過）

### 3.2 每日攤提帶寬（防極端）
花費必須維持每日均勻，不能前半月花完、後半月沒攤提。

- **APP 產品**：`每日理論 = 月預算 / 當月天數`，每日攤提總和可在 ±30% 之間
  - 例：AV9 月預算 50 萬 / 30 天 = 16,666，每日可接受區間 **11,666 ~ 21,666**
- **小島型產品**：同公式，但只能 **±0.5%**
- **混合共購**：APP 產品與小島產品可以共購同一筆廣告。此時小島那一側仍須嚴格滿足 ±0.5%，APP 那一側滿足 ±30%。任一側違反即視為違規配置。

### 3.3 貨幣
- 廣告採買：**人民幣計價**
- 產品總預算、報表：**台幣計價**
- 每筆廣告採買時要填「當下匯率」，換算該筆的台幣總額並鎖定
- 匯率分兩種（通常一個月改一次）：
  - **支出匯率**：預設 4.7
  - **收入匯率**：預設 4.5

### 3.4 廣告生命週期事件（段式紀錄）

一筆廣告從下單到結束可能經歷多次調整。每次調整**不改寫原段**，而是**關閉當前段、開新段**，新段 `renewal_of` 指回前段 `ad.id`。每段都標註 `renewal_reason`，形成完整時間軸。

| `renewal_reason` | 觸發情境 | 必填輸入 | 效果 |
|---|---|---|---|
| `初始` | 新增廣告 | code/name/RMB/匯率/起迄/攤提天/權重 | 建立第 0 段 |
| `續費` | 原段到期，完整延續 | 新期間/新 RMB/新匯率/新攤提天 | 開新段 |
| `續費匯率` | 續費但只改匯率 | 新匯率+生效日 | 新段 `amount_twd = amount_cny × 新匯率` 重算 |
| `漲價` / `降價` | 續費但只改 RMB | 新 RMB+生效日 | 新段其他不變 |
| `權重調整` | 成效調整後 | 生效日+新 weights dict | 新段金額不變、只改 weights |
| `送天數` | 臨時贈送期間 | 起迄+暫停計費的產品清單 | 該產品權重暫時歸 0 的新段 |
| `送天數結束` | 贈送結束恢復 | 生效日 | 權重恢復的新段 |
| `轉移` | 一般↔破圈 / 轉小島 | 生效日+源 pv→目 pv+金額 | 源 pv 結束、目 pv 新段 |
| `批量匯率調整` | **歷史用**（月末 4.5→4.7 全面換匯） | 生效日+新匯率 | 所有活躍段 ratio-scale（**新系統不再產生**） |

**關鍵規則：**
- **歷史已扣的台幣金額以當時匯率為準，永不追溯重算**
- **未來新廣告的匯率變動只在續費當下套用**，不做 bulk 回溯調整
  - 過去有 bulk「4.5→4.7 整體調整」屬歷史資料特性，新系統不產生
- 續費時沿用 `ad_code` / `ad_name` / `group`，但金額/匯率/起迄/攤提天/權重 5 個欄位都可獨立變動
- `renewal_of` + `renewal_reason` 一起看就是該廣告完整的生命時間軸
- 跨月攤提時匯率沿用該段**開始時鎖定**的匯率，不因月份匯率換了而重算（續費段用續費當下新匯率）

### 3.4.1 歷史資料匯入策略（舊 xlsx，一次性）

舊 xlsx 複雜性：
- 產品分頁是「誰付款」的帳本，真正產品歸屬在 `預估費用` 分頁的主表 + `BF:CP` 事件日誌
- `權重變動紀錄` 分頁記錄權重隨時間變化，但不完整（4/25 之後多數人工調整未登）
- 4/25 以後主表值是人工修正的，以 `samples/ground_truth_2026-YY.json` 為最終權威

**做法：`ground_truth`-driven allocator**（[samples/allocator.py](samples/allocator.py)）

1. 掃 xlsx 產品分頁所有跟目標月份 `[月初, 次月初)` 有 overlap 的列，取 metadata（code/name/group/I/J/M/Q/P）
2. 去重同 `(sheet, ad_code, pid, start, end)`（xlsx 偶有重複列，取 row 號較大者代表最新編輯）
3. 套 **破圈 override**（xlsx E 欄誤標修正，詳見「已知誤標清單」）
4. 每日用 `權重變動紀錄` 取 `(code, pid, date)` 當日最新權重；找不到用 xlsx P 欄 fallback
5. 每日將 `ground_truth[date][pid]` 按「active 廣告 × weight」比例分配給各筆廣告 — 保證每日 sum = ground_truth
6. 以 `ad_code` 為 timeline key 合併所有 sheet 的貢獻到單一時序；連續同 pid 分配（±1 TWD 精度）合併成一段 segment；切段時開新記錄用 `renewal_of` 鏈接

**結果（2026-04）：0/330 個 per-day outlier > ±3 TWD，月合計最大偏差 -11 TWD**（來自 ground_truth 給無廣告日的 1 TWD placeholder 噪音，非實際廣告）

**已知 xlsx E 欄誤標**（匯入時硬編碼 override 破圈歸屬）：

| code | xlsx 分頁 E 欄 | 實際應歸屬 | 佐證 |
|---|---|---|---|
| `998 大象軟件庫` | `E='JK'` | `jk_poquan` | 4/1 JK 破圈 4701 對應 998 daily 4700 |
| `999 蛋蛋分享庫` | `E='AV9'` | `av9_poquan` | r423 col64 `AV9+破圈 +7833.33` / r438 col63 `-7833.33` |
| `984 月谣` | `E='AV9'` | `av9_poquan` | r448 col63 `AV9-破圈 -111.23` |
| `1000 白鷺學園` | `E='AV9'` | `av9_poquan` | r456 col63 `AV9-破圈 -26.63` |

**歷史特有事件語意**（allocator 透過 ground_truth 直接涵蓋，新系統不產生）：
- `送天數-` + `送天數結束+` 配對：中間期間該產品對該廣告的貢獻歸 0
- `送天數結束+調降`：結束時同時調降金額（不完全恢復），以事件 + 值為新段金額
- `調整價格`：續費時漲/降價，按事件 + 值開新段
- `匯率調整`（bulk）：按 ratio 等比例縮放所有活躍段
- `XXX轉Y`：`轉移` 事件，源 pv 結束、目 pv 開新段

### 3.5 跨月攤提
廣告起迄跨月時（例：4/25 起、攤提 30 天）：
- **歸屬**：按實際攤提日切開計入各月（4 月算 4/25~4/30 那幾天，5 月算 5/1~5/24）
- **匯率**：沿用**開始時鎖定**的匯率，不管月份匯率換了沒
  - 例外：若此廣告後續被續費，續費段用**續費當下的新匯率**（§3.4）
- **月預算佔用**：每月的預算只需覆蓋該月實際攤提那幾天的金額（4 月預算只扣 6 天份，不用預留整筆 30 天份）

---

## 4. 資料模型

### 4.1 Product（產品）
- `id`
- `name`（愛威奶 / 愛威奶破圈 / 健康 / 健康破圈 / 黃油圈 / 破解吧 / 汁婦寶 / 萬精游 / 磨欲爽 / 色軟庫 / 熊貓巴士）
- `type`（`app` / `island`）
- `monthly_budget_twd`（台幣）
- `performance_targets[]`（可多個，使用者自訂）：
  - `name`（例：`CPI`）
  - `formula`（例：`花費 / 不重複安裝數`）
  - `goal_value`（目標值，例：CPI ≤ 30）
  - `direction`（`lower_better` / `higher_better`）

### 4.2 Ad（廣告採買記錄 — 每筆實際上是「一段」）

一筆 `Ad` 記錄實際上代表**一段穩定時期的合約狀態**。任何調整都透過「關舊段、開新段、用 `renewal_of` 鏈接」來表達（§3.4）。

- `id`
- `ad_code`（廣告代碼，例：70 / st215 / dh1001；同代碼的多段共用）
- `ad_name`（廣告名稱；同代碼的多段共用）
- `group`（廣告分組；同代碼的多段共用）
- `amount_cny`（人民幣金額；每段可獨立變動）
- `exchange_rate`（此段採買當下的鎖定匯率）
- `amount_twd`（自動 = `amount_cny × exchange_rate`）
- `start_date`（含）
- `end_date`（不含）
- `amortize_days`（**手動填**）
- `daily_amort_twd`（自動 = `amount_twd / amortize_days`）
- `purchase_mode`（`shared` 共購 / `independent` 獨立採買）
- `weights`：`{ product_id: percent }`（共購時加總 = 100；獨立採買時只有一個產品 = 100；允許小數百分比以避免累積 rounding 誤差）
- `performance_data`（後續填入實際成效）
- `renewal_of`（指向前一段 `ad.id`；初始段為 `null`）
- `renewal_reason`（enum：`初始` / `續費` / `續費匯率` / `漲價` / `降價` / `權重調整` / `送天數` / `送天數結束` / `轉移` / `批量匯率調整`；詳見 §3.4）

### 4.3 DailySpend（每日花費快照，可由 Ad 推算）
- `date`
- `product_id`
- `ad_id`
- `amount_twd`

---

## 5. 功能需求

### 5.1 產品面板
- 輸入：產品名稱、當月總預算（台幣）
- 顯示：當月已配置每日攤提曲線、剩餘預算、剩餘可配置空間

### 5.2 新增廣告
輸入：廣告代碼、廣告名稱、廣告分組、廣告金額（人民幣）、當下匯率、開始日、結束日、**攤提天數**
- 自動產出：建議權重分配 + 建議採買日期
- 可手動覆寫權重與日期
- 系統依「各產品剩餘預算」與「每日攤提帶寬剩餘空間」演算最佳配置

### 5.3 反向建議（先有日期、再找廣告）
- 輸入：想購買的日期
- 系統回建議：這天哪幾個產品可以一起買、總金額建議多少、權重怎麼分
- 使用者拿此去外部找符合價位的廣告

### 5.3.5 每週成效匯入流程
- 前端在 Google Sheets 裡建一個「**成效輸入**」分頁（按鈕「🗂️ 建立成效輸入分頁」會用正確表頭初始化一個空表）
- 使用者每週把廣告平台匯出的資料貼到此分頁（欄位：資料起始日 / 資料結束日 / **廣告代碼** / 對應產品 / 廣告分組 / 13 個指標。外部來源不會給「花費」，故不在輸入欄）
- **廣告代碼 fuzzy match**：不同後台的代碼會有 `dh` 前綴或英文字尾的變體
  - 例：`st100` = `dhst100` = `st100H` = `dhst100H`；不會等於 `st1002` / `dhst1002J`
  - 規則：去掉可選 `dh` 前綴 + 去掉可選英文字尾後比對基本碼（不分大小寫）
  - 函式：`normalizeAdCode(code)` 在 [app/io/sheets-schema.js](app/io/sheets-schema.js)
- 按「📥 附加本週成效」：
  1. 讀取「成效輸入」分頁
  2. 驗證每列：對應產品存在（可用 id 或中文名）、期間是有效 YYYY-MM-DD；用基本碼 fuzzy match 找系統內廣告，配合「該產品有權重 + 期間重疊」自動擇一
  3. 通過驗證的列顯示在預覽 modal，錯誤列條列說明（含診斷：「同基本碼找到 N 筆，但無對應產品權重或期間不重疊」）
  4. 使用者按「匯入 N 筆」→ 依 `(ad_code, product_id)` 去重併進 `performance_data`（採系統內標準代碼，不存外部變體），同步推回「成效資料」分頁
  5. 選擇性清空「成效輸入」，準備下一週貼資料
- 此流程**不走一般 push/pull**，避免把暫存區的未匯入資料覆寫掉

### 5.4 成效驅動權重調整
- 每個產品有一或多個 `performance_targets`（使用者自填「目標名稱」+「公式」+「目標值」+「方向」）
- 公式可用的變數（來自每個廣告 × 每個產品的成效回填）：
  - `花費`、`不重複安裝數`、`廠商安裝`、`不重複首頁開啟數`、`不重複活躍用戶數`
  - `首儲訂單數`、`首儲購買金額`、`加總訂單數`、`加總購買金額`
  - `所有渠道不重複安裝數`、`所有渠道不重複活躍用戶數`
  - `總活躍用戶`、`總下載點擊`、`事件計數`
- 使用者事後回填每個廣告在每個產品的上述實際成效
- 系統判定：以**是否達到該產品的目標值**為依據
  - 達標/超標 → 提高該廣告於該產品的權重
  - 未達標 → 降低（可以降到 0，直接淘汰也可以）
- **單次調整無上限**，**不需保留最低權重**
- 唯一硬限制：調整後仍須符合 §3 預算規則（月結容差 + 每日攤提帶寬）
- 產出「調整後權重建議」表，供使用者確認

### 5.5 確認 → Todo List
權重調整/分配建議結果旁邊要有 **「確認」鍵**：
- 按下確認 → 自動新增一筆 Todo（提醒去改**連結分流設定**）
- 操作完成 → 回 Todo 按「完成」

### 5.6 已購廣告清單
顯示：廣告代碼、名稱、分組、金額（人民幣+台幣）、起迄、攤提天數、每日攤提、各產品權重
同代碼多段預設**摺疊顯示為同一列**，展開後才看到各段 `renewal_reason` 與細節。

### 5.7 廣告生命週期 UI 操作（必備）

每筆廣告詳情頁都要有這幾個操作按鈕。每個按鈕都是「關舊段、開新段、`renewal_of` 鏈」的前端觸發。

| 按鈕 | 彈窗表單欄位 | 產出 `renewal_reason` |
|---|---|---|
| **續費** | 新起迄、新 RMB、新匯率（預設=上次鎖定）、新攤提天、新權重（預設=上段） | `續費`（若全改）/ `續費匯率` / `漲價` / `降價` |
| **權重調整** | 生效日、新 weights dict | `權重調整` |
| **送天數** | 起迄日、暫停計費的產品清單 | 自動產 2 段：`送天數` + `送天數結束`（到結束日時開第三段恢復） |
| **轉移** | 生效日、源 pv、目 pv、金額 | 雙邊：源段 `轉移`、目段 `轉移`（新段） |
| **結束** | 提前結束日 | 把 `end_date` 改到當日，不開新段 |

**P1 — 減少誤操作**：
- 輸入匯率時旁邊顯示「當前支出匯率 4.7（上次變動 2025-02-01）」
- 新增/續費時即時跑預算檢查（月結容差 + 每日攤提帶寬）
- 每個生命週期事件寫 audit log（誰、時間、改了什麼、原因）

**P1 — 時間軸視圖**：廣告詳情頁顯示該 `ad_code` 所有段的時間軸，每段標示 `renewal_reason` + 關鍵差異（如「匯率 4.5→4.7」/「權重 AV9 30%→50%」）

**P2 — 智能化**：
- 到期前 3 天自動產 Todo「`st215` 即將到期，是否續費？」
- 反向建議：選日期→算剩餘預算空間→反推該買什麼金額
- 匯率批量調整工具：一鍵對當天所有活躍段開新段（但此操作**只在歷史匯入時用**，新系統不鼓勵）

---

## 6. 報表與匯出

### 6.1 看得到的資訊
- 每個產品 **每日花費**（台幣）
- 每個**廣告分組**每日總花費
- 每個產品當月總花費 vs 預算差額
- 每日全產品總攤提曲線

### 6.2 匯出
- **CSV**：按產品分頁（AV9 分頁、JK 分頁…，每頁列出該產品所有廣告與權重）
- **JSON**：完整資料結構
- 每日攤提/花費計算也要能匯出

### 6.3 匯入
- **CSV 匯入**既有廣告資料
- 欄位：費用、開始日、結束日、攤提天數、適用產品、權重

---

## 7. 儲存與同步

### 7.1 前端
- 預設用 **localStorage** 操作（零延遲、離線可用）
- 提供兩顆按鈕：
  - **☁️ 同步到 Google Sheets**：整份資料推上去
  - **⬇️ 從 Sheets 拉下來**：覆寫本地
- 不做自動同步，避免衝突

### 7.2 Google Sheets 串接
- 採用 **Apps Script Web App** 作為中介（執行身分=擁有者、存取=任何人）
- Apps Script 只提供 3 個通用動作，**不含任何業務 schema**：
  - `{ action: "ping" }` → 檢查連線
  - `{ action: "writeTable", sheetName, headers, rows }` → 清掉並覆寫指定分頁
  - `{ action: "readTable", sheetName }` → 讀回 `{headers, rows}`
- 前端呼叫時用 `multipart/form-data`（欄位名 `payload` = JSON 字串），原因：
  - 避免跨域 POST preflight
  - 避免 Apps Script 302 轉址時 `Content-Type: application/json` 被改成 GET 導致回 405
- 每個請求都要帶 `token`，後端比對 Apps Script 內硬寫的 `SECRET` 常數
- push = 前端遍歷 schema（`app/io/sheets-schema.js`）逐個分頁呼叫 `writeTable`
- pull = 前端遍歷 schema 呼叫 `readTable`，然後 `assembleFromTables` 組回 state
- pull 前會先自動下載一份當前 localStorage JSON 備份，避免誤覆蓋

### 7.3 Sheet 結構（正規化）
單一真實來源，後端只存正規化資料，人肉瀏覽用衍生報表。

| 分頁 | 用途 | 主要欄位 |
|---|---|---|
| `Products` | 產品主檔 | id, name, type(app/island), monthly_budget_twd |
| `PerformanceTargets` | 產品成效目標 | id, product_id, name, formula, goal_value, direction |
| `Ads` | 廣告主檔 | id, ad_code, ad_name, group, amount_cny, exchange_rate, amount_twd, start_date, end_date, amortize_days, daily_amort_twd, purchase_mode, renewal_of |
| `AdWeights` | 廣告×產品權重 | ad_id, product_id, weight_percent |
| `PerformanceData` | 成效回填（系統維護，來源是 `PerformanceInput`） | ad_id, product_id, period_start, period_end, 花費, 不重複安裝數, 廠商安裝, ... (14 變數) |
| `PerformanceInput`（成效輸入） | **使用者每週貼資料的暫存區**，不在一般 push/pull 循環中，由 📥 附加本週成效 按鈕單獨處理 | 廣告代碼, 產品代碼, 期間起, 期間迄, + 14 變數 |
| `DailySpend` | 每日花費（選配，也可由 Ads+AdWeights 即時算） | date, product_id, ad_id, amount_twd |
| `每日攤提覆寫` | 歷史資料覆寫（由舊 xlsx 預估費用主表匯入）。儀表板優先用這個，沒有才算自 Ads | 日期, 產品ID, 每日攤提(台幣), 備註 |
| `Todos` | 確認後待辦 | id, created_at, action_type, payload, status |
| `Settings` | 當月設定 | key, value（含支出匯率、收入匯率、月份起迄等） |
| `Report_by_Product`（衍生） | 給人眼看，由 Apps Script 依正規化資料產生 | 每個產品一個區塊，列廣告與權重 |

> 為什麼不直接對齊 `2025採買資料庫.xlsx` 的產品分頁格式：同一筆廣告會出現在多個產品分頁，雙向同步時容易衝突。正規化後 CRUD 單純、衝突少；需要舊格式時用 §6.2 的「按產品 CSV 匯出」或 `Report_by_Product` 衍生表補齊。

---

## 8. 技術方向

- **單一靜態網頁**（HTML + JS + CSS，無後端）
- 資料層：localStorage + Google Sheets API
- 可直接用瀏覽器開啟使用

---

## 9. 驗證資料來源

- `2025採買資料庫.xlsx`
  - 產品分頁（AV9 / JK / 黃油圈=HYC / 破解吧 / 汁婦寶 / 萬精游 / 磨欲爽 / 色軟庫 / 熊貓=熊貓巴士）：全部廣告原始資料
  - 預估費用分頁 **B ~ AK 欄**：4 月每日攤提人工算出結果（系統產出應與此對得上）
  - 預估費用分頁 **BF ~ CP 欄**：歷次調整後結果
- `2025_打款_權重紀錄清單.xlsx`
  - 權重紀錄分頁：所有權重變化紀錄（理論上等同上述 BF~CP 欄，可交叉對比）

### 9.1 xlsx 解析規則（匯入舊資料時必看）

**資料列從第 4 列開始**。第 2 列是表頭、第 3 列是彙總 / 元資料，都要略過。

**欄位對應**（以 1 為起點）：
| 欄 | 舊表頭 | 系統欄位 |
|---|---|---|
| C (3) | 編號 | `ad_code` |
| D (4) | 名稱 | `ad_name` |
| E (5) | 產品 | 判斷破圈用（見下） |
| F (6) | 文案 | — |
| G (7) | RMB | `amount_cny` |
| H (8) | 平均一天 | —（系統自己重算） |
| I (9) | 起始 | `start_date` |
| J (10) | 結束 | `end_date` |
| **M (13)** | **廣告天數** | **`amortize_days`（手動值，優先採用）** |
| N (14) | 分類 | `group` |
| P (16) | 分配權重 | 權重值（0~1 的小數，需 × 100 轉成整數 %） |

**破圈拆分規則**：舊 xlsx 分頁名仍是舊代號（AV9 / JK / 黃油圈 / 熊貓 …），而且把 AV9 + 愛威奶破圈、JK + 健康破圈 **寫在同一個產品分頁裡**。E 欄內容形式為 `AV9-破圈` 或 `JK-破圈`（含連字符），匯入時要拆成兩個獨立產品：

| xlsx 分頁 | E 欄含「破圈」 | 系統對應產品（id / 名稱） |
|---|---|---|
| AV9 | 否 | `AV9` 愛威奶 |
| AV9 | 是 | `av9_poquan` 愛威奶破圈 |
| JK | 否 | `JK` 健康 |
| JK | 是 | `jk_poquan` 健康破圈 |
| 黃油圈 | — | `HYC` 黃油圈 |
| 破解吧 | — | `PJ8` 破解吧 |
| 汁婦寶 | — | `ZFB` 汁婦寶 |
| 萬精游 | — | `OJI` 萬精游 |
| 磨欲爽 | — | `MYS` 磨欲爽 |
| 色軟庫 | — | `XRK` 色軟庫 |
| 熊貓 | — | `BS` 熊貓巴士 |

拆出的破圈產品在系統內是**完全獨立的產品**（有自己月預算、自己的帶寬、自己的權重欄位），不和母產品共用任何計算規則。

**合併規則**：以 `ad_code` 為 timeline key，合併所有 sheet 的每日 pid 貢獻到單一時序，然後依連續同 pid 分配（±1 TWD 精度）切段。同一分頁內同 `(code, pid, start, end)` 有多列時，取 row 號大者（最新編輯）。

**參考實作**：
- [samples/allocator.py](samples/allocator.py)：`ground_truth`-driven allocator，產出 `samples/buyads_2026-04.json`（4月已達 0 outlier）/ `samples/buyads_2026-05.json`
- [samples/ground_truth_2026-04.json](samples/ground_truth_2026-04.json) / [samples/ground_truth_2026-05.json](samples/ground_truth_2026-05.json)：使用者手動校正的每日每產品權威值，allocator 以此為分配目標
- [samples/reverse_apr1_from_sheets.py](samples/reverse_apr1_from_sheets.py)：獨立驗證 4/1 baseline（PJ8/ZFB/OJI/MYS/XRK/BS 6 個產品 100% 對上主表）

---

## 10. 已決議規則（歷史答覆彙整）

- **#3 破圈歸屬**：愛威奶破圈 / 健康破圈 視為**完全獨立的產品**（自己月預算、自己帶寬、自己權重），不和愛威奶 / 健康共用任何規則。舊 xlsx 把它們和母產品寫在同一頁，匯入時依 E 欄「破圈」關鍵字+ 4 筆誤標 override 拆分（§3.4.1、§9.1）。
- **#1 成效目標**：使用者自訂「目標名稱 + 計算公式 + 目標值 + 方向」，公式可引用 §5.4 列出的 14 個變數。每個產品可多個指標。
- **#2 自動調權演算法**：以「是否達到該產品的目標值」作好壞依據；單次調整無上限、可歸零淘汰；唯一硬限制是調整後仍須符合 §3 預算規則。
- **#5 段式紀錄**：所有「續費 / 調權 / 送天數 / 轉移 / 匯率變動」**一律關舊段開新段**，用 `renewal_of` 鏈 + `renewal_reason` 欄紀錄原因。不改舊段；歷史已扣金額永不追溯重算（§3.4、§4.2）。
- **#4 APP × 小島混合共購**：可以；但小島那一側仍須嚴格 ±0.5%，APP 那一側 ±30%，任一側違反即違規（見 §3.2）。
- **#6 匯率變動方式**：未來**不做 bulk 回溯調整**，只在續費當下套用新匯率到新段（§3.4）。舊 xlsx 裡的 `匯率調整` bulk 事件屬歷史特性，allocator 用 ratio-scale 處理但新系統不產生。
- **Google Sheets 串接**：用 Apps Script Web App（執行身分=擁有者、存取=任何人），前端以 fetch JSON action 溝通（§7.2）。
- **Sheet 結構**：正規化（Products / Ads / AdWeights / …）作為單一真實來源；人肉瀏覽另走衍生報表（§7.3）。
- **廣告分組 / 分類**：統一為「**廣告分組**」一個詞（單層），欄位名 `group`，報表依此彙總。
- **跨月攤提**：按實際攤提日切到各月，月預算只佔該月那幾天；匯率沿用鎖定匯率；續費段用續費當下的新匯率（§3.5）。
- **歷史匯入精度**：以使用者提供的 `samples/ground_truth_2026-YY.json` 每日每產品值為終極權威；allocator 做到 2026-04 的 0/330 per-day outlier > ±3 TWD。

---

## 11. 待釐清問題（實作前再確認）

（目前全部已釐清，實作進行中若有新問題再補。）
