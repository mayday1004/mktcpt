# 廣告投放管理系統 - 規格文件

## 0. 回覆答案時
以繁體中文回答為主，不要中英文夾雜

## 1. 產品列表

> **產品清單由 Google Sheets「產品」分頁(或 UI「+ 新增產品」)維護,沒有 hardcode 預設值**。「重設全部資料」會清成空清單,下一輪同步從 Sheets 拉回真資料。下表是目前實務上會出現的產品,供參考。

我們有兩種產品,規則不一樣。

### APP 類產品(每天花費可上下浮動 30%)
| 產品名稱 | 產品代碼 | 備註 |
|---|---|---|
| 愛威奶 | `AV9` | |
| 愛威奶破圈 | `av9_poquan` | 自己一個產品,規則跟愛威奶一樣 |
| 健康 | `JK` | |
| 健康破圈 | `jk_poquan` | 自己一個產品,規則跟健康一樣 |
| 黃油圈 | `HYC` | |

> 「破圈」雖然名字裡有母產品(像愛威奶破圈),但它是一個完全獨立的產品:有自己的月預算、自己的權重、自己的計算規則,跟愛威奶完全不共用。
>
> 唯一要特別處理的地方,只有「從舊試算表匯入歷史資料」時要把它從母產品分頁裡拆出來,詳見 §9.1。

### 小島型產品(每天花費只能上下浮動 0.5%)
| 產品名稱 | 產品代碼 |
|---|---|
| 破解吧 | `PJ8` |
| 汁婦寶 | `ZFB` |
| 萬精游 | `OJI` |
| 磨欲爽 | `MYS` |
| 色軟庫 | `XRK` |
| 熊貓巴士 | `BS` |

---

## 2. 核心計算規則

### 2.1 每日攤提(每天平均花多少)
- **「攤提天數」預設自動換算 = (結束日 − 開始日)**,但**只在「新增 / 續費」模式**自動同步;**編輯既有廣告**(已存在的段)時,改起迄日**不會**自動覆寫攤提天數,因為:
  - 「權重調整」會把一段切兩半(例:30 天合約在 4/27 切 → 4/16-4/27 + 4/27-5/16)
  - 兩段的 `amortize_days` **必須沿用原合約的 30 天**,不能改成各自的區間天數(11/19)
  - 否則每段會把整個 `amount_twd` 攤在自己的天數上 → 兩段加總會雙倍計算
- 編輯模式如果使用者要手動改 am 仍可改,但要自己確保跟邏輯一致
- 編輯彈窗顯示提示:
  - `am == span` → ✓
  - `am > span` → 「這段是切出來的片段,am 沿用原合約 — 正常」
  - `am < span` → 「⚠️ 重複計算 X× 台幣金額,通常是錯誤資料」
- **每日攤提金額 = 廣告總金額(台幣) ÷ 攤提天數**
- **算錢的區間:從開始日當天開始算錢,結束日當天不算錢**
- 例子:9 萬人民幣的廣告分 30 天攤提 → 每天攤提 3,000 人民幣 (再用匯率換成台幣)

### 2.2 共購分權(多個產品一起買同一個廣告)
一個廣告可以由好幾個產品一起付錢,每個產品分一個百分比,當天攤提的錢就照百分比拆。

例子:9 萬廣告分 30 天,每天攤提 3,000
- 產品 A 出 50% → 每天負擔 1,500
- 產品 B 出 30% → 每天負擔 900
- 產品 C 出 10% → 每天負擔 300
- 產品 D 出 10% → 每天負擔 300

### 2.3 獨立採買(同一個廣告代碼,但各產品自己各買一份)
有些情況雖然是同一個廣告代碼或廣告名稱,但**不同產品其實是各自獨立去買**,每一份各自佔 100%。

例子:廣告代碼 70「成人色導航」
- AV9 自己買一份,AV9 占 100%
- JK 自己買一份,JK 占 100%

資料結構要能分清楚這筆是「共購」還是「獨立採買」。

### 2.4 權重(百分比)的最小單位
- 最少改動單位是 **1%**
- 同一筆廣告,所有產品的百分比加起來要等於 100%(共購);獨立採買的話每份各自 100%

---

## 3. 預算規則

### 3.1 月底結算的容許誤差
- **APP 產品**:少花最多可以差 **6 萬台幣**;超花最多可以差 **2 萬台幣**。
- **小島型產品**:少花最多可以差 **2 萬台幣**;超花最多可以差 **1 萬台幣**。
- 超過上述容差就視為違規或需要處理;APP 的日花費範圍較鬆,所以月度少花容差也較大。

### 3.2 每日攤提的「合理範圍」(防止亂花)
不能前半個月狂花、後半個月沒得花。每天的花費要平均一點,落在合理範圍內。

- **APP 產品**:`每天理論花費 = 月預算 ÷ 當月天數`,實際每天可以差到 ±30%
  - 例:AV9 月預算 50 萬 ÷ 30 天 = 16,666,所以每天合理範圍是 **11,666 ~ 21,666**
- **小島型產品**:同樣的公式,但只能差 **±0.5%**(很嚴格)
- **混合共購**:APP 產品和小島產品可以一起出錢買同一筆廣告。這時候小島那邊還是要嚴格守住 ±0.5%,APP 那邊守 ±30%。任何一邊破了範圍,這個分配就算違規。

### 3.3 貨幣
- 廣告採買時:用**人民幣**計價
- 產品月預算、各種報表:用**台幣**計價
- 每筆廣告下單時要填「當下的匯率」,把人民幣換成台幣的金額並鎖定
- 匯率分兩種(通常一個月才會改一次):
  - **支出匯率**(我們付錢用):預設 4.7
  - **收入匯率**(收錢用):預設 4.5

### 3.4 廣告生命週期(用「分段」記錄變化)

一筆廣告從開始買到結束,中間可能會調整很多次(續費、改價錢、改權重等)。每次調整**不去改寫舊資料**,而是「把現在這段結束、開新一段」,新段裡的 `renewal_of` 欄位指回上一段的 ID,這樣就形成完整的時間軸。每一段都會在 `renewal_reason` 欄位裡寫變動原因。

| `renewal_reason` 值 | 什麼時候會用 | 要填什麼 | 效果 |
|---|---|---|---|
| `初始` | 新增廣告 | 代碼/名稱/人民幣金額/匯率/起迄日/攤提天數/權重 | 建立第一段 |
| `續費` | 舊段到期,接著繼續買 | 新的期間/金額/匯率/攤提天數 | 開新一段 |
| `續費匯率` | 續費時只改匯率 | 新匯率 + 生效日 | 新段的台幣金額重算 = 人民幣金額 × 新匯率 |
| `匯率調漲` / `匯率調降` | 續費時只改人民幣金額 | 新金額 + 生效日 | 新段其他不變 |
| `權重調整` | 看完成效後改權重 | 生效日 + 新權重表 | 新段金額不變,只改權重 |
| `拆t改名` | 權重變動觸發「同家族母+破圈共存」拆 pair,系統把整支廣告改名 `stXXX` ↔ `stXXXt` | 系統自動產生(無 UI 入口)| 同一支廣告的 `ad_code` 變動,所有歷史段一律 follow 新名稱;段的 `code_at_creation` 保留段建立當下的代碼(§5.7.2) |
| `送天數` | **舊系統用過的標籤**:臨時送幾天 | — | **新系統不再產生**;實務上「送天數」就是續費時把開始日填晚幾天,造成同代碼兩段中間有空檔,沒有獨立的段(§5.8) |
| `送天數結束` | **舊系統用過的標籤**:送完恢復 | — | 同上,新系統不再產生 |
| `轉移` | **舊系統用過的標籤**:一般↔破圈 / 轉到小島 | — | **新系統不再產生**;一律改用「權重調整」,跨產品的搬遷細節寫在新段的備註欄(例:「AV9 50% → 愛威奶破圈 50%」) |
| `批量匯率調整` | **歷史專用**(月底全面 4.5→4.7 換匯) | 生效日 + 新匯率 | 所有正在跑的段一起按比例縮放(**新系統不再產生**) |

**最重要的幾個規則:**
- **歷史已經扣掉的台幣金額,用當時的匯率為準,以後不會回頭重算**
- **未來新廣告的匯率變動,只在「續費當下」套用**,不會回頭去改舊資料
  - 過去那種「全部一起 4.5 換 4.7」的批量改法,屬於歷史資料特性,新系統不會做
- 續費時,廣告代碼、廣告名稱、廣告分組會沿用上一段;但金額、匯率、起迄日、攤提天數、權重這 5 樣可以各自獨立改
- 把所有段串起來看(用 `renewal_of` 鏈接),就是這個廣告的完整時間軸
- 跨月攤提時,匯率用該段「開始時鎖定」的匯率,就算月份換了匯率也不重算(續費段才用續費當下的新匯率)
- **「轉移」和「權重調整」合併**:新系統只會產生「權重調整」。歷史段裡如果原本是「轉移」就保留原標籤(時間軸顯示「轉移(舊事件)」),不會回頭去改

### 3.4.1 歷史資料怎麼從舊試算表匯入(只做一次)

舊試算表的複雜之處:
- 每個產品分頁是「誰付錢」的帳本,但真正每天該付多少在「預估費用」分頁的主表 + 右邊事件日誌(BF~CP 欄)
- 「權重變動紀錄」分頁雖然有歷史權重,但不完整(4/25 之後很多人工調整沒登記)
- 4/25 以後主表的數字是人工修正過的,以 `samples/ground_truth_2026-YY.json` 為最終正確答案

**做法:用「正確答案」回推分配**(實作在 [samples/allocator.py](samples/allocator.py))

1. 掃舊試算表所有產品分頁,挑出跟目標月份有時間重疊的列,抓基本資料(代碼/名稱/分組/起迄日/攤提天/權重等欄位)
2. 同一筆 (分頁、廣告代碼、產品、起迄日) 如果重複出現,取列號比較大的(代表最新編輯)
3. 套上**破圈修正規則**(舊試算表的 E 欄有 4 筆寫錯,要硬改;清單見下方)
4. 每天用「權重變動紀錄」找該日當天最新的權重;找不到就用試算表 P 欄當備案
5. 每天把「正確答案」(該日該產品總金額)按「正在跑的廣告 × 權重比例」分配給每一筆廣告 - 保證每天加起來就等於正確答案
6. 用廣告代碼當主索引,把所有分頁的每日貢獻合併成一條時間線;連續同一個產品分配的日子(誤差在 ±1 台幣內)合併成一段;切段時開新段並用 `renewal_of` 鏈接

**驗證結果(2026-04 月份):330 個 (日 × 產品) 組合裡,沒有任何一個誤差超過 ±3 台幣;整月加總最大誤差只有 -11 台幣**(這 11 元來自正確答案表給沒廣告的日子放了一個 1 元的小數字,不是真實廣告)

**舊試算表 E 欄已知的 4 筆寫錯**(匯入時硬改成正確的破圈歸屬):

| 廣告代碼 | 舊試算表 E 欄寫的 | 正確應該是 | 怎麼確認的 |
|---|---|---|---|
| `998 大象軟件庫` | `JK` | `jk_poquan` | 4/1 JK 破圈 4701 對得上 998 當天 4700 |
| `999 蛋蛋分享庫` | `AV9` | `av9_poquan` | 事件日誌第 423 列第 64 欄寫「AV9+破圈 +7833.33」、第 438 列第 63 欄寫「-7833.33」 |
| `984 月谣` | `AV9` | `av9_poquan` | 事件日誌第 448 列第 63 欄寫「AV9-破圈 -111.23」 |
| `1000 白鷺學園` | `AV9` | `av9_poquan` | 事件日誌第 456 列第 63 欄寫「AV9-破圈 -26.63」 |

**舊系統特有的事件含義**(分配程式透過「正確答案」直接覆蓋,新系統不會產生):
- `送天數-` 配對 `送天數結束+`:中間那段期間,該產品對該廣告的貢獻是 0
- `送天數結束+調降`:結束時順便調低金額(沒完全恢復),以事件裡寫的數字為新段金額
- `調整價格`:續費時漲價或降價,以事件值為新段金額
- `匯率調整`(整批):所有正在跑的段按比例縮放
- `XXX轉Y`:舊系統的「轉移」事件,源產品結束、目標產品開新段

### 3.5 跨月攤提
廣告起迄日跨月時(例:4/25 開始、攤提 30 天):
- **怎麼歸到各月**:照實際攤提日切開算各月份(4 月算 4/25~4/30 那 6 天的份,5 月算 5/1~5/24 那 24 天的份)
- **匯率**:用「開始時鎖定」的匯率,就算月份換了匯率也不重算
  - 例外:這支廣告之後續費的話,續費那一段用「續費當下的新匯率」(§3.4)
- **月預算佔用**:每個月的預算只要顧到該月實際攤提那幾天的金額(4 月預算只扣 6 天份,不用一次預留整筆 30 天)

---

## 4. 資料模型

### 4.1 Product(產品)
- `id`(產品代碼,使用者自訂)
- `name`(名稱,使用者自訂)
- `type`:`app` 或 `island`(APP 類 / 小島類)
- `no_band`:布林。`true` 表示「不檢查每日帶寬」(原本 `av9_poquan` / `jk_poquan` 寫死的例外行為,現在改成 per-product 旗標,可以在 Sheets「產品」分頁的「不檢查每日帶寬」欄填 `Y`,或在產品編輯彈窗勾選)。預設 `false`。
- `monthly_budget_twd`(月預算,台幣)
- `performance_targets[]`(成效目標,一個產品可以有好幾個,使用者自訂):
  - `name`(目標名稱,例:`CPI`)
  - `formula`(公式,例:`花費 / 不重複安裝數`)
  - `goal_value`(目標數值,例:CPI ≤ 30)
  - `direction`:`lower_better`(越低越好)或 `higher_better`(越高越好)

### 4.2 Ad(廣告採買記錄 - 每筆其實是「一段」)

每一筆 `Ad` 記錄代表「一段穩定期間的合約狀態」。任何調整都用「結束舊段、開新段、用 `renewal_of` 鏈接」來表達(§3.4)。

- `id`(這一段的編號)
- `ad_code`(廣告代碼,例:70 / st215 / dh1001;同代碼的多段共用)
- `ad_name`(廣告名稱;同代碼多段共用)
- `group`(廣告分組;同代碼多段共用)
- `amount_cny`(人民幣金額;每段可以各自不同)
- `exchange_rate`(這段下單當下鎖定的匯率)
- `amount_twd`(自動算 = `amount_cny × exchange_rate`)
- `start_date`(開始日,含當天)
- `end_date`(結束日,不含當天)
- `amortize_days`(攤提天數,**手動填**)
- `daily_amort_twd`(自動算 = `amount_twd ÷ amortize_days`)
- `purchase_mode`:`shared`(共購)或 `independent`(獨立採買)
- `weights`:`{ 產品代碼: 百分比 }`
  - **單支沒配對**(`split_pair_id` 為 null):加總 = 100
  - **配對(split pair)**(parent + t-variant 共用 `split_pair_id`):**單支內加總不一定 = 100**,跨配對兩支加總 = 100(每個產品 weight 即「占整體 %」)。理由:§5.7.2「依使用者原輸入存,不歸一」,UI 顯示「總額 X RMB · 一般 80% · 破圈 20%」就是 sum 出來
  - 允許小數百分比避免累積誤差
- `split_pair_id`(選填):配對識別碼。同一個 pair 的 parent + t-variant 共用此 ID,儀表板靠它聚合家族(不靠代碼 t 後綴判定)。`null` 代表單支廣告
- `split_role`(選填):`parent` / `t_variant`,標示在 pair 中的角色。`null` = 不在 pair 內
- `code_at_creation`(選填):段建立當下的 `ad_code`。後續因「拆 t 改名」而 `ad_code` 變動時,這個欄位保留原始值,UI 時間軸與縮網址歷史對照用
- `performance_data`(之後填入的實際成效)
- `renewal_of`(指向上一段的 `ad.id`;第一段為 `null`)
- `renewal_reason`(可選值:`初始` / `續費` / `續費匯率` / `匯率調漲` / `匯率調降` / `權重調整` / `拆t改名` / `送天數` / `送天數結束` / `轉移` / `批量匯率調整`;詳見 §3.4)

### 4.3 DailySpend(每日花費快照,可以從 Ad 推算)
- `date`(日期)
- `product_id`(產品代碼)
- `ad_id`(廣告段編號)
- `amount_twd`(當天台幣金額)

---

## 5. 功能需求

### 5.1 產品面板
- 輸入:產品名稱、當月總預算(台幣)
- 顯示:當月已配置的每日攤提曲線、剩多少預算、還能配多少空間

### 5.2 新增廣告
輸入:廣告代碼、廣告名稱、廣告分組、廣告金額(人民幣)、當下匯率、開始日、結束日、**攤提天數**
- 系統自動建議權重分配 + 建議採買日期
- 可以手動覆寫權重和日期
- 系統會看「各產品剩多少預算」和「每日攤提還能塞多少」演算最佳配置

### 5.3 反向建議(先有日期、再找廣告)
- 輸入:想買廣告的日期
- 系統回建議:這天哪幾個產品可以一起買、總金額抓多少、權重怎麼分
- 使用者拿這個建議去外面找符合價位的廣告

### 5.3.5 每週成效匯入流程
- 系統會在 Google 試算表裡建一個「**成效輸入**」分頁(按「🗂️ 建立成效輸入分頁」就會用正確的表頭建一個空的)
- 使用者每週把廣告平台匯出的資料貼到這個分頁(欄位:資料起始日 / 資料結束日 / **廣告代碼** / 對應產品 / 廣告分組 / 13 個指標。外部來源不會給「花費」,所以輸入欄不放)
- **廣告代碼模糊比對**:不同後台會幫同一支廣告加上 noise 變體;但有兩個尾綴是**語意尾綴**,必須保留為獨立廣告
  - **等價類舉例**(每行內視為同一支):
    - 一般 `st100` = `dhst100` = `st100H` = `st100h` = `st100J` = `st100j`(前綴 `dh` + 任意單一字母尾,皆大小寫不分)
    - 第二位 `st100dh` = `st100DH` = `dhst100dh`(尾綴 `dh` 為語意,保留;但 `dh` 前綴仍可砍)
    - 破圈 `st100t` = `st100T`(尾綴 `t` 為語意,保留)
  - **三個等價類兩兩不等**:`st100` ≠ `st100dh` ≠ `st100t`
  - 規則(`normalizeAdCode` in [app/io/sheets-schema.js](app/io/sheets-schema.js)):
    1. 砍可選的 `dh` 前綴(大小寫不分)
    2. 尾巴整段英文,如果剛好命中 `t` 或 `dh`(大小寫不分)→ 保留為語意尾綴
    3. 其餘任何純字母尾(`H` / `J` / `ABC` 等)→ 後台 noise,砍掉
  - 命名約定:**未來廣告代碼不要混用「語意尾綴 + 後台 noise 尾綴」**(例 `st100tH` / `st100Hdh`),會造成歧義(目前演算法會當 noise 砍掉)。如果系統內已經建了 `st100t`,後台應該叫它 `st100t` / `st100T` / `dhst100t`,不要再加額外尾綴。
  - **何時需要 `t` 尾綴**(2026-05 重訂):**唯一觸發條件 = 同支廣告的 weights 同時含「某家族的母 + 破圈」**(`AV9` + `av9_poquan` 或 `JK` + `jk_poquan`)。觸發時系統自動拆 pair,所有破圈進 t-variant、所有一般留 parent(§5.7.2)。其餘情境一律**不加 t**:
    1. 純一般(可多家族,例 `{AV9:30, JK:20, PJ8:50}`) → 一支 `st123`
    2. 純破圈(可多家族,例 `{av9_poquan:60, jk_poquan:40}`) → 一支 `st123`(**不加 t**;儀表板靠 weights 內容判定,不靠代碼)
    3. 跨家族混合,沒撞母產品(例 `{AV9:60, jk_poquan:40}`,沒 av9_poquan) → 一支 `st123`
    4. 同家族碰撞(例 `{AV9:50, av9_poquan:50}`) → **觸發拆 t**:parent `st123` + t-variant `st123t`,共用 `split_pair_id`
    - 一旦拆 pair 觸發,**所有破圈一律進 t-variant**(包括跟母產品沒撞的破圈,例 jk_poquan 在前述 AV9 家族碰撞情境下也會放進 `st123t`)
    - 解除拆 t(例:一般那側退場、只剩破圈) → **不自動還原代碼**,`st123t` 保持(改名是單向的,避免來回變動)
  - 家族卡片邏輯(儀表板 / 成效報表):**不靠代碼 t 後綴判定**,改靠 weights 內容 + `split_pair_id` 聚合。跨家族純破圈共存的廣告(例 `st123` av9_poquan 70% + jk_poquan 30%)會同時被 AV9 家族卡 + JK 家族卡計入,各依該產品 weight 份額
  - 範例:
    - 純一般採買 → `st123`
    - 純破圈採買(任何家族單側或多家族共存) → `st123`(**不加 t**!)
    - 純破圈 → 後續加入同家族母產品 → 觸發拆 t:整支 `st123` 改名為 `st123t`,新建 `st123` 給一般側,共用 `split_pair_id`(§5.7.2)
    - 從 day 1 就同家族混合(`AV9:70, av9_poquan:30`) → 系統依新增廣告當下偵測到觸發條件,直接建 pair:parent `st123` `{AV9:70}` + t-variant `st123t` `{av9_poquan:30}`
    - 跨家族混合不撞母(`AV9:60, jk_poquan:40`) → 一支 `st123`,不拆
- 按「📥 附加本週成效」:
  1. 讀取「成效輸入」分頁
  2. 驗證每一列:對應產品要存在(可用代碼或中文名)、期間要是有效的 YYYY-MM-DD 格式;用基本碼模糊比對找系統內的廣告,搭配「該產品有權重 + 期間重疊」自動選一筆
  3. 通過驗證的列顯示在預覽彈窗,有錯的列條列說明(含診斷:「同基本碼找到 N 筆,但都沒對應產品的權重或期間沒重疊」)
  4. 使用者按「匯入 N 筆」→ 依 (廣告代碼, 產品代碼) 去重,合併進 `performance_data`(用系統內標準代碼存,不存外部變體),同時推回「成效資料」分頁
  5. 可選擇清空「成效輸入」分頁,準備下一週貼資料
- 這個流程**不走一般的同步推/拉**,避免把暫存區還沒匯入的資料蓋掉

### 5.4 用成效驅動權重調整
- 每個產品有一個或多個成效目標(使用者自填「目標名稱 + 公式 + 目標值 + 方向」)
- 公式裡可以用的變數(每筆廣告 × 每個產品的成效回填):
  - `花費`、`不重複安裝數`、`廠商安裝`、`不重複首頁開啟數`、`不重複活躍用戶數`
  - `首儲訂單數`、`首儲購買金額`、`加總訂單數`、`加總購買金額`
  - `所有渠道不重複安裝數`、`所有渠道不重複活躍用戶數`
  - `總活躍用戶`、`總下載點擊`、`事件計數`
- 使用者事後回填每筆廣告在每個產品的實際成效
- 系統判定:看**有沒有達到該產品的目標值**
  - 達標或超標 → 提高該廣告在該產品的權重
  - 沒達標 → 降低(可以降到 0,直接淘汰也行)
- **單次調整沒上限**,**也不必保留最低權重**
- 唯一硬規定:調整後仍要符合 §3 預算規則(月結容差 + 每日攤提範圍)
- 產出「調整後權重建議」表給使用者確認

#### 5.4.1 影響預覽:只算「打勾要套用」的廣告

「權重調整」頁上方的「影響預覽」面板(顯示各產品月攤提 / 日峰值 / 是否在合理範圍內)**只把使用者打勾「✓ 套用」的廣告納入計算**,沒打勾的就維持原權重不變。

- 系統可能一次推薦調整 N 筆廣告,但使用者只想套用其中幾筆 → 預覽要立刻反映「只套用這幾筆」之後的數字
- 「手動覆寫權重」(暫存改動)只有在這筆廣告**也有打勾**時才生效;手改但沒打勾 = 視為「還在猶豫」,不算進預覽
- UI 視覺:沒打勾的列**整列灰一點**(淡底色),讓使用者一眼看出「目前預覽的是哪幾筆」
- 「重置為系統建議」只重置手動改動,不動打勾狀態

#### 5.4.2 跨產品調權:把 0% 的產品也納入考量

**問題**:原本演算法只在「這筆廣告有權重的產品之間」重新分配。例:小黃書原本 AV9 50% / JK 50%,AV9 成效差,但削掉 AV9 的權重沒地方可以塞(其他產品本來這支廣告就 0%),系統就只能不調整。

**新做法**:當某個產品 X 預估月攤提**少花超過 §3.1 容差**(APP 6 萬、小島 2 萬)時,系統會主動建議**把 X 加進其他廣告**(就算這支廣告原本 X = 0%)。

**演算法**:
1. 先跑既有的「同廣告內、有權重的產品之間互調」邏輯
2. 對每個「少花超過容差」的產品 X:
   - 找所有「目前正在跑」的廣告 Y
   - 從 Y 的權重裡**借出 ΔW 給 X**,借的順序:
     - **優先借**:Y 上「多花最嚴重的產品」(月攤提 - 月預算 最大者)— 一舉雙得
     - **次選借**:Y 上「成效最爛的產品」(成效分數比例最低)
     - **不採用**:等比例每個都扣一點
   - ΔW 由 §3.2 範圍雙邊檢查決定上限:
     - X 加進去後,X 在當日的合計攤提(含其他廣告)必須在合理範圍內(不超 upper、不低 lower)
     - 被借的產品扣完後,也必須在合理範圍內
   - 任何一邊超出範圍 → 砍小 ΔW 直到剛好不超出;ΔW 砍到 0 就跳過這支廣告
3. 每筆廣告的權重總和 = 100% 強制(原本就有的邏輯)

**範圍不放寬**:小島仍然 ±0.5%(很窄),加 5% 權重就可能超出 → 那就不加,維持原狀。系統不會為了補預算而違反 §3.2。

**UI 呈現**:
- 每支廣告的權重編輯區**預設只顯示「該廣告原本有權重 + 系統建議要加的」產品**,下面有「⊕ 展開全部產品」按鈕,按下會展開全部 11 個產品(含 0%),讓使用者**手動加進任何產品**。摺疊狀態會記住(切廣告或重畫畫面都保留)。
- 系統「主動加進去」的產品:在欄位旁標示「**+5%**(綠色)」表示這是新加的,使用者可以一眼分辨「原本就有的調整」vs「系統新加進來的」
- **預算空間提示**(跟「主動建議」門檻分開):只要產品有預算還沒用完,UI 就顯示「剩 X 台幣可花」之類的提示,讓使用者**手動**決定要不要加進某支廣告
  - 觸發系統「自動建議」:少花超過 §3.1 容差
  - 顯示「視覺提示」:少花 > 0 就會顯示

**範例**(使用者實例):
> 小黃書原本 AV9 50% / JK 50%。HYC 還有預算(少花 > 6 萬),AV9 成效差。HYC 沒有多到能全吃 AV9 的 50%。
>
> 系統建議:**AV9 35% / JK 50% / HYC 15%**(HYC 加到剛好碰到自己的範圍上限為止),其他產品(av9_poquan、jk_poquan、PJ8、ZFB、OJI、MYS、XRK、BS、黃油圈無關)顯示為 **0%(可編輯)**,讓使用者彈性手動加。

**主題不符**:系統不會判斷「色情廣告丟健康類產品奇不奇怪」(沒有主題分類資料)。使用者看到不合理的建議,**手動把該產品權重改回 0** 即可。

### 5.5 確認 → 待辦清單 + 撤回功能(2026-05 重寫)
權重調整或分配建議的結果旁邊要有「**確認**」按鈕:
- 按下確認 → 自動新增一筆待辦(提醒去改**連結分流設定**)
- 操作完成 → 回到待辦按「完成」

#### 5.5.1 撤回(undo)
每一筆**會異動廣告資料**的 todo 都會帶 `undo_payload` 欄位,記錄套用變動之前的廣告快照,讓使用者後悔時可以一鍵還原:

```js
todo = {
  ...,
  undo_payload: {
    ad_snapshots: [Ad, ...],   // 套用前被改到的廣告完整 deep-clone
    added_ad_ids: ["id1", ...] // 套用時新建的 ad.id 列表
  }
}
```

**撤回流程**(在待辦清單按「↩ 撤回」):
1. 跳確認對話框,提示「會還原資料變動,且此筆之後對同一廣告的調整也會一併回滾」
2. 確認後:`applyUndo(state, payload)` 會把 `added_ad_ids` 從 `state.ads` 刪掉、把 `ad_snapshots` 寫回原位
3. 同時刪掉這筆 todo

**沒有 `undo_payload` 的 todo**(例如手動新增的待辦 / 既有舊資料):按鈕顯示為「刪」,只刪除提醒,不還原資料。

**支援撤回的 action_type**:
- `補日花費缺口` — gift-day-fix-modal 套用
- `成效調整權重` — perf-adjust 套用
- `手動改權重` — ads 詳情頁「權重調整」彈窗
- `新增廣告` — 新建廣告
- `淘汰廣告` — 廣告淘汰

**限制**:撤回只還原 `state.ads` 範圍。`budget_changes`、`settings`、`monthly_budgets` 等其他資料不在範圍內(目前所有相關 action 也只動 ads,所以夠用)。

### 5.6 已購廣告清單
顯示:廣告代碼、名稱、分組、金額(人民幣 + 台幣)、起迄日、攤提天數、每日攤提、各產品權重
同代碼的多段預設**摺疊成同一列**,展開才會看到各段的變動原因和細節。

### 5.7 廣告生命週期 UI 操作(必備)

每筆廣告詳情頁的按鈕 layout(2026-05 重整):

```
[編輯]  [權重調整]  [⋯]
                    ├ 續費
                    ├ 結束
                    ├ 鎖定狀態 ▸
                    └ 淘汰
```

「編輯」和「權重調整」當主按鈕(高頻);其餘進 `⋯` menu。

> **拆出破圈/一般分流按鈕已全部移除**(新增廣告彈窗 + 詳情頁皆已移除)。改成由「權重調整」/「新增廣告」依 weights 內容**自動偵測同家族碰撞**並拆 pair(§5.7.2)。

每個按鈕都是「結束舊段、開新段、用 `renewal_of` 鏈接」的前端入口:

| 按鈕 | 彈窗要填的欄位 | 產生的 `renewal_reason` |
|---|---|---|
| **編輯** | 改基本欄位(代碼/名稱/分組/匯率/起迄日等),不改權重 | 不開新段(直接更新當前段) |
| **權重調整** | 生效日、新權重、**備註**(選填) | `權重調整`(若觸發拆 pair 同時產生 `拆t改名`)|
| **續費** | 新起迄、新人民幣金額、新匯率(預設 = 上次鎖定)、新攤提天、新權重(預設 = 上段) | `續費`(全改的話)/ `續費匯率` / `匯率調漲` / `匯率調降` |
| **結束** | 提前結束日 | 把 `end_date` 改到當日,不開新段 |

#### 5.7.1 廣告鎖定狀態三段(2026-05 新增)

每支廣告有一個鎖定狀態,**只影響系統「自動建議」**(成效驅動 / 補日花費缺口),手動編輯永遠放行。

| 狀態 | 識別碼 | 自動演算法 source pool | 自動演算法的提案 | 適用情境 |
|---|---|---|---|---|
| 🔓 **自由** | `lock_perf_adjust=false, lock_full=false` | ✅ 可被 fractional 借權重 | 一般權重 split | 預設 |
| 🔒 **鎖權重** | `lock_perf_adjust=true, lock_full=false` | ⚠️ 只接受「整桶搬」(必須是 single-100% ad) | 整桶搬到別產品 | 廣告必須維持單一產品 100%,但仍可在不同產品間整桶移動 |
| 🚫 **禁止挪動** | `lock_full=true` | ❌ 完全跳過 | 成效爛 → 顯示「建議淘汰」 | 廣告綁死在某個產品,完全不能挪 |

**Schema 欄位**(`Ad`):
- `lock_perf_adjust: boolean` — 鎖權重比例(自動不分權重)
- `lock_full: boolean` — 禁止挪動(完全不納入自動建議)

`lock_full = true` 時 `lock_perf_adjust` 也會同步為 `true`(更嚴格的狀態 ⊃ 較鬆的狀態),確保所有「鎖權重」相關檢查都成立。

**演算法觸發條件**:
- **planAdjustments** (gift-day-fix-modal) — 借權重補缺口時:
  - 🚫 跳過
  - 🔒 + 廣告是 single-product 100% → 接受作為「整桶搬」候選(`kind: "transfer"`、`deltaW: 100`)
  - 🔒 + 廣告為混合權重(非 single-100%)→ 跳過(整桶搬不適用)
  - 自由 → 走 fractional split(原邏輯)
- **suggestProductAdjustments + buildAdPivot** (perf-adjust) — 成效驅動調整時:
  - 🚫 跳過,顯示鎖定 reason 文字「🚫 禁止挪動(自動不動)」;若所有權重產品都 `ratio < 0.3` 仍進入「建議淘汰」分支
  - 🔒 + single-product 100% + 該產品 `score.ratio < 0.3` → 設定 `entry.transferTarget` 並覆寫 `suggestedWeights = { target: 100 }`,目標 = 「最少花的產品」(B1: monthSpent / budget 最低者,排除自己與破圈)
  - 🔒 + 混合權重 → 跳過(維持原樣)

**UI 操作**:
- **廣告詳情頁 → ⋯ 更多動作 menu**:三個按鈕分別設定鎖定狀態,當前狀態的按鈕灰掉
- **權重調整頁 → 🔒 欄**:點圖示循環切換 `🔓 → 🔒 → 🚫 → 🔓`,設成鎖權重 / 禁止挪動時自動清掉該行的 ✓ 套用勾選與 pending 覆寫

#### 5.7.2 自動拆 t / 解除拆 t(2026-05 新增)

**觸發條件(同 §5.3.5)**:同支廣告 weights 同時含「某家族母 + 破圈」(`AV9` + `av9_poquan` 或 `JK` + `jk_poquan`)。

**觸發時機**:任何寫入路徑(新增廣告、權重調整、續費)在 commit 之前都跑同家族碰撞偵測。

**處理流程**(以「`st123` av9_poquan 100% 跑 5/10-6/10,5/14 加入 AV9 50%」為例):

1. 偵測 5/14 起新權重 `{AV9:50, av9_poquan:50}` → AV9 家族碰撞
2. 對 source ad(原 `st123`):
   - 5/10-5/14 段保留 `{av9_poquan:100}`,`ad_code` 跟著整支改名為 `st123t`
   - 開新段 5/14-6/10 `{av9_poquan:50}`,`renewal_reason='權重調整'` + `renewal_reason='拆t改名'` 並存(同步 emit 兩個事件 segment,或合併用備註欄記)
3. 新建 ad(parent 一般側):
   - `ad_code='st123'`、5/14-6/10、`{AV9:50}`、`renewal_reason='初始'`
4. 兩支共用新生 `split_pair_id`,`split_role` 分別 `parent` / `t_variant`

**權重結構(跨 pair 不歸一)**:
- t-variant 段 `{av9_poquan:50}` 加總 = 50
- parent 段 `{AV9:50}` 加總 = 50
- 跨 pair 加總 = 100,代表整支合約的 100%
- daily 分配時:`產品 X 的日攤提 = pair 總 daily × (X 的 weight / 100)`,跨段聚合

**段層欄位**:
- `code_at_creation`:段建立當下的 `ad_code`。例:5/10-5/14 段 `code_at_creation='st123'`,廣告改名後 `ad_code='st123t'` 但歷史對照仍能查到原代碼
- 縮網址頁面(§7.x 縮網址功能)依 `code_at_creation` 顯示段建立時的代碼,而不是當前 `ad_code`

**解除拆 t**(例:5/25 AV9 退場,只剩 av9_poquan):
- 一般那側 ad(`st123`) 自然結束(end_date = 5/25),不刪除
- t-variant ad(`st123t`) 維持 `st123t`,**不自動還原為 `st123`**
- 理由:改名是單向的,避免來回變動造成資料抖動

**鎖定狀態(🔒/🚫)交互**:
- 自動拆 t 屬於「使用者手動編輯觸發的結構性操作」,不算自動演算法
- 對 🔒 / 🚫 鎖定的廣告做手動權重調整 → **仍會觸發拆 t,但跳確認框**:
  > 🔒 此廣告為鎖權重狀態,您手動把權重改成「一般 + 破圈混合」,系統將自動建立 `st123t` 配對(分流到 t-variant)。確定嗎? [取消] [確定拆分]
- 確認後正常拆 pair;取消則回滾權重變更

**家族卡片邏輯**:
- 不再靠代碼 t 後綴判定母 vs 破圈
- 改靠 `weights` 內容判定:廣告的每筆 weight 各算入該產品的家族卡片
- 同 `split_pair_id` 的兩支自動聚合顯示為一個合約

**配對重平衡(`rebalanceSplitPair`,既有)**:
- 對任一支做權重調整 → 重算雙方 amount + weights
- 若某產品從 t-variant 改回 parent(或反之),金額自動流動到正確側
- 詳見 [app/domain/split-pair.js](app/domain/split-pair.js)

> **「轉移」按鈕已經移除**(§3.4)。原本「一般↔破圈 / 轉小島」的場景現在統一用「權重調整」處理:直接把源產品權重改成 0、目標產品權重加上去;建議在備註欄寫「來源 → 目的」方便日後查閱。歷史段裡 `renewal_reason='轉移'` 的時間軸標籤保留為「轉移(舊事件)」。

> **「送天數」按鈕已經移除**(§3.4 / §5.8)。實務上未採買空檔(舊稱送天數空窗)就是續費時起始日填晚 N 天造成的兩段中間空檔,沒有獨立段。系統不再單看空檔本身,而是統一偵測「日花費低於下限」與「APP 月攤提少花 > 6 萬」這兩類問題,在「需要決策」區與權重調整頁 banner 提供「一鍵調整」的修補建議(§5.8)。

**P1 — 減少誤操作**:
- 輸入匯率時旁邊顯示「目前支出匯率 4.7(上次變動 2025-02-01)」
- 新增 / 續費時即時跑預算檢查(月結容差 + 每日攤提範圍)
- 每個生命週期事件寫操作紀錄(誰、什麼時間、改了什麼、原因)

**P1 — 時間軸視圖**:廣告詳情頁顯示該廣告代碼所有段的時間軸,每段標出變動原因 + 關鍵差異(例:「匯率 4.5→4.7」/「權重 AV9 30%→50%」)

**P2 — 智能化**:
- 到期前 3 天自動產一筆待辦「`st215` 即將到期,要不要續費?」
- 反向建議:選日期 → 算剩餘預算空間 → 反推該買什麼金額
- 匯率批量調整工具:一鍵對當天所有正在跑的段開新段(但這個操作**只在歷史匯入時用**,新系統不鼓勵)

### 5.8 廣告調整建議(2026-05 重寫,原「送天數空窗處理」更名)

#### 5.8.0 用詞與核心觀念

「**未採買空檔**」(舊稱「送天數空窗」)= 同一個廣告代碼,連續兩段之間「下一段開始日 > 上一段結束日」造成的空檔。成因有兩種,系統不區分,用同一套邏輯處理:

| 成因 | 範例 | 是否在系統內留痕 |
|---|---|---|
| **廠商送天數** | 糖心 5/10 到期,續費起始填 5/16(廠商免費送 5/10~5/15) | 兩段間有空檔 |
| **斷檔再採買** | 糖心 5/10 到期不續費,5/16 想通了又買回 | 兩段間有空檔 |

**沒有獨立 UI 按鈕**,沿用既有「續費」按鈕填日期即可(把續費起始日填晚 N 天就會自然產生空檔)。

#### 5.8.1 真正觸發「需要調整」的條件(不單看空檔)

未採買空檔本身**不一定**要調整 — 如果空檔期間所有產品花費仍在建議下限以上,就被動接受少花。系統實際偵測的是**三類**會觸發「廣告調整建議」的情況,不限是否與空檔有關:

| 偵測項 | 觸發條件 | 顯示位置 |
|---|---|---|
| ⚠️ **產品日花費低於下限** | 任一產品任一未來日的有效日花費 < 建議日花費下限 | 概覽「需要決策」+ 權重調整頁 banner |
| 💰 **APP 月攤提少花 > 6 萬** | 任一 APP 產品本月預估月攤提 < 月預算 - 6 萬 | 同上 |
| 📅 **未採買空檔(資訊)** | 任何未來空檔 | 概覽 + 廣告列表上方資訊卡(只通知不警示) |

**重要差異**:
- 純空檔但所有產品都還在下限以上 → **資訊卡照顯示,不出現警示,不建議調整**(被動接受少花)
- 不論成因(空檔、其他廣告到期、月初分配偏低…),只要有產品日花費 < 下限 → 觸發警示

#### 5.8.2 調整建議演算法(planAdjustments,優先序四 pass)

掃描範圍:今天(含)到當月底。

##### 規則

1. **小島 daily shortfall** 優先補 — §3.2 ±0.5% 嚴格,雙邊不破
2. **APP monthly underspend > 6 萬** — 把該產品日花費推到 band.upper;仍不足以收斂 underspend ≤ 6 萬 → 允許**突破上限**
3. **APP daily shortfall** 最後補 — §3.2 ±30%,屬軟性目標;能補就補,不能補就優先確保月預算與小島下限
4. **0% 產品也納入分配** — 沒被使用者鎖定 (`!lock_perf_adjust`) + 該日 active 的廣告,即使目標產品在該廣告權重 = 0% 也可加進去

##### 候選來源

- **來源廣告**:沒被鎖 + 該日 active
- **來源產品**(從來源廣告現有 weight > 0 的非目標產品挑):
  - 優先序 1:多花最嚴重(月攤提 - 月預算 > checkMonthlyTotal 的 over tolerance)
  - 優先序 3:其他(中性)

##### 雙邊夾擠(capByBands)

對每筆候選,在來源廣告的整個 active 期間(從生效日到 ad.end_date 或月底,取較早)逐日檢查:

- **來源端**:每天削後不能破來源產品的 band.lower
- **目標端**(pass 1/2/3a):每天加後不能破目標產品的 band.upper
- **目標端**(pass 3b — APP 突破上限):跳過此檢查,允許超出 upper

任一邊夾到 0 就跳過該筆候選。

##### 偽碼

```
activeFixes = []   # 已 schedule 的調整(累計效果)
planned = []       # 對外回傳

# 共用 helpers:
#   effectiveBaseline(pid, day) = 原始 grid + activeFixes 累積後的有效日花費
#   effectiveWeights(ad, day) = 原始 weights + activeFixes 累積後的權重
#   pickBestSource(targetPid, day) = 找最佳(廣告, 來源產品)
#   capByBands(ad, sourcePid, targetPid, requestedW, day, options) = 雙邊夾擠後合法 ΔW
#   tryFix(...) = 配上一筆,push 進 activeFixes/planned

# Pass 1:小島 daily shortfall
for 小島產品 X:
  for 每一天:
    if effectiveBaseline(X, day) < band.lower:
      candidate = pickBestSource(X, day)
      if candidate: tryFix(候選, X, 缺額, priority=1)

# Pass 2a:APP monthly underspend > 6 萬,推到 upper
for APP 產品 X:
  underspend = budget - projectMonthly(X)
  if underspend > 60000:
    for 每一天:
      if underspend ≤ 60000: break
      if effectiveBaseline(X, day) < band.upper:
        candidate = pickBestSource(X, day)
        if candidate: tryFix(候選, X, 餘量, priority=2)
        underspend = budget - projectMonthly(X)

# Pass 2b:APP 仍 underspend > 6 萬 → 允許突破 upper
for APP 產品 X:
  if budget - projectMonthly(X) > 60000:
    for 每一天:
      if underspend ≤ 60000: break
      candidate = pickBestSource(X, day)
      if candidate: tryFix(候選, X, 平均分配的需求量, priority=2,
                            options={ allowTargetUpperBreak: true })

# Pass 3:APP daily shortfall
for APP 產品 X:
  for 每一天:
    if effectiveBaseline(X, day) < band.lower:
      candidate = pickBestSource(X, day)
      if candidate: tryFix(候選, X, 缺額, priority=3)
```

**注意**:capByBands 會在加進去前確認「整段廣告活著的未來日」都不爆 upper,所以 pass 1/2/3a 不需要事後 LIFO 挪回 — 不合法的調整一開始就不會被加進去。

#### 5.8.3 套用 UI

按警示「→ 一鍵調整」會打開**廣告調整建議**彈窗:

- 標題:🎁 **廣告調整建議**
- 統計:`X 筆「小島補下限」、Y 筆「APP 補月預算」、Z 筆「APP 補下限」`
- 表格列出每筆 `{ ad, effective, sourcePid, targetPid, deltaW, priority }`,**每筆有勾選框,預設全勾**
- 顏色標籤:小島補下限 = 紅、APP 補下限 = 橘、APP 補月預算 = 藍
- 確認 → 對勾選的調整產生所有「權重調整」段:
  - `renewal_reason = '權重調整'`
  - 備註欄寫上原因(例:「補小島 破解吧 至下限 — 從健康借(5/10 起)」)
- **建立一筆待辦**:`action_type = '補日花費缺口'`,描述含每筆細節 + 「請至連結後台調整」

##### 「找不到來源廣告」備援文案

如果 detect 有問題但 plan 為空(所有 candidate 都被 capByBands 夾到 0,或根本沒 candidate),改顯示:

> 🎁 **廣告調整建議**
>
> 偵測到 X 個產品未來日花費低於建議下限,合計缺 Yk TWD。
> 目前所有產品都還有預算空間,無法透過調整權重讓產品花費達標。
> 👉 **建議改成直接採買新廣告補上這個缺口。**
>
> `[前往採買建議]` `[了解]`

「前往採買建議」按鈕會用 `sessionStorage.buyads_reverse_prefill_date` 把最早的相關日期帶到反向建議頁,使用者一進去就直接看到該日的採買建議。

---

## 6. 報表與匯出

### 6.1 看得到的資訊
- 每個產品**每日花費**(台幣)
- 每個**廣告分組**每日總花費
- 每個產品當月總花費 vs 預算差額
- 每日全產品總攤提曲線

### 6.2 匯出
- **CSV**:按產品分頁(AV9 一頁、JK 一頁…,每頁列出該產品所有廣告與權重)
- **JSON**:完整資料結構
- 每日攤提 / 花費計算也要能匯出

### 6.3 匯入
- **CSV 匯入**既有廣告資料
- 欄位:費用、開始日、結束日、攤提天數、適用產品、權重

---

## 7. 儲存與同步

### 7.1 前端
- 預設用**瀏覽器本機儲存**操作(零延遲、離線可用)
- **自動 row-level CAS 同步**(2026-05 改版):state 變動後 debounce 5 秒推上 Sheets、每 30 秒拉一次;不再有「☁️ 推」/「⬇️ 拉」整批按鈕(留設定頁的「手動同步」當 fallback)
- 多人協作衝突:有衝突時右下角出現 ⚠️ banner,點開 modal 逐筆解決(用我的 / 用對方的 / 逐欄選擇);衝突未解前自動同步暫停

### 7.2 Google 試算表串接(v4 協定)
- 用 **Apps Script 網頁應用程式**當中介(執行身分 = 擁有者、存取權 = 任何人)
- Apps Script 提供以下 actions,**不含任何業務邏輯**:
  - `{ action: "ping" }` → 健康檢查
  - `{ action: "readMeta" }` → 拿 server_version + last_modified_at(輕量短路 pull 用)
  - `{ action: "readTable", sheetName }` → 讀單張 sheet
  - `{ action: "readAllTables", sheetNames }` → 一次拉多張 sheet(合 12 round trip 為 1)
  - `{ action: "upsertRows", sheetName, headers, rows }` → **CAS 寫入**:rows 內 `_version` 為 expected,server 不符就回 `conflicts` 不寫入;符合就 +1 寫入
  - `{ action: "writeTable", sheetName, headers, rows }` → 整片覆寫(救援用)
- 前端呼叫時用表單格式(`multipart/form-data`,欄位名 `payload` = JSON 字串),原因:
  - 避免跨域 POST 預檢
  - 避免 Apps Script 轉址時 `Content-Type: application/json` 被改成 GET 導致回 405
- 每個請求都要帶 `token`,後端比對 Apps Script 裡寫死的 `SECRET` 常數
- 每張資料分頁的最後 4 欄固定為隱性 metadata:`_id` / `_updated_at` / `_deleted` / `_version`

### 7.2.1 衝突偵測與解決流程
- **Push 路徑(CAS)**:client `update()` 觸發 → debounce 5s → 推 `upsertRows`,每筆 row 帶 `_version = sync_meta._version`(client 上次看到的 server 版本);server 比對:
  - 版本對得上 → 寫入,server bump version+1,client 更新 sync_meta
  - 版本不符 → server 回 `conflicts[]`(含 current_row + current_version)→ client 進 conflict-store
- **Pull 路徑**:`readAllTables` 拉回所有 row → LWW 合併,但加一道偵測:
  - 若 (server `_updated_at > sync_meta._updated_at`)**且**(本機 fingerprint ≠ sync_meta.fingerprint)→ 衝突(雙方都改過)→ 不套用,進 conflict-store
  - 否則照原本 LWW(只有一邊改 → 接受該邊)
- **解衝突 modal**([app/io/conflict-resolver.js](app/io/conflict-resolver.js)):
  - 列出每筆衝突的「欄位 / 我的值 / 對方值 / radio」per-field diff
  - 三顆快捷:🟢 全用我的、🔵 全用對方、🟡 套用上方逐欄選擇
  - 全域快捷:全部用我的 / 全部用對方
  - 「用我的」= 用對方的 `_version` 當新 expected,標 `__force_push__` fingerprint → 下次 push 重推、CAS 通過
  - 「用對方的」= 把對方 row 套進本機 state,sync_meta 對齊
- **暫停 auto-sync**:衝突佇列非空時 orchestrator 跳過,使用者解完佇列歸零後自動恢復
- **錯誤 logging**:同步層所有事件(push 成功/失敗、衝突發生、pull 衝突、網路錯誤、retry)都進 console + in-memory ring buffer。DevTools 打 `__buyadsLog()` 看最近 200 筆 — 給「我明明儲存了卻又被改回去」這類抱怨除錯用([app/lib/sync-log.js](app/lib/sync-log.js))

### 7.2.2 部署版本 gate(2026-05 新增)

問題:同事的瀏覽器在我們 deploy 完之後第一次打開,有兩種情境會推爛資料到雲端 —
(a) **冷啟動**:抓到新 JS 但 localStorage 還是舊版 build 寫的 shape,新邏輯誤判 → 推錯;
(b) **長 tab**:同事的分頁本來就開著沒 refresh,持續用 in-memory 的舊 JS。

對應兩道檢查([app/lib/version-gate.js](app/lib/version-gate.js)):

| 檢查 | 觸發時機 | 行為 |
|---|---|---|
| **A. 冷啟動 gate** (`runColdStartGate`) | [app/state.js](app/state.js) load 之前同步執行 | 比對 `localStorage.buyads_build` vs build-time 注入的 `__BUYADS_BUILD__`,不同 → 清掉 `buyads_state_v1` / `buyads_sync_meta_v1` / `buyads_server_version_v1` / `buyads_undo_v1` / `buyads_conflicts_v1`,sessionStorage 留訊息給 toast |
| **B. 長 tab watch** (`initLongTabWatch`) | DOMContentLoaded 後啟動,5 秒後第一次跑、之後每 30 秒 | fetch `/version.txt`,server build ≠ 自己的 BUILD → 右下角 banner「🔄 偵測到新版本 → 立即重整 / 稍後」,**不自動 reload**(避免吞掉沒推的改動) |

**build id 來源**:[build.js](build.js) 依序試 `process.env.RAILWAY_GIT_COMMIT_SHA` → `GIT_SHA` → `COMMIT_SHA` → `git rev-parse --short=10 HEAD` → `Date.now().toString(36)`(全跑光的 fallback)。同次 commit 重建會有同 id → 不會誤觸發 gate;不同 commit 則 id 變動。

**注入機制**:
- 走 esbuild `define: { __BUYADS_BUILD__: JSON.stringify(buildId) }`,值會被 bake 進 bundle
- 同時寫 `dist/version.txt`(內容就是 buildId)給長 tab fetch
- [app/lib/build-info.js](app/lib/build-info.js):`BUILD` 常數讀 `__BUYADS_BUILD__`,dev(raw modules 沒走 bundle)fallback 為 `"dev"`,gate 自動跳過

**Caddy 快取規則**:`version.txt` 跟 `*.html` 一樣設 `Cache-Control: no-cache`(Caddyfile)。`*.js`/`*.css` 維持 1 小時快取,但 build.js 早就在 `<script src="app.js?v=<hash>">` 加 hash query 強制 cache-bust,所以 deploy 完同事一 refresh 就會抓到新 JS。

### 7.3 試算表結構(正規化)
單一資料來源,後端只存正規化資料,人工瀏覽用衍生報表。

| 分頁 | 用途 | 主要欄位 |
|---|---|---|
| `Products` | 產品主檔 | id, name, type(app/island), monthly_budget_twd |
| `PerformanceTargets` | 產品成效目標 | id, product_id, name, formula, goal_value, direction |
| `Ads` | 廣告主檔 | id, ad_code, ad_name, group, amount_cny, exchange_rate, amount_twd, start_date, end_date, amortize_days, daily_amort_twd, purchase_mode, renewal_of |
| `AdWeights` | 廣告 × 產品權重 | ad_id, product_id, weight_percent |
| `PerformanceData` | 成效回填(系統維護,來源是 `PerformanceInput`) | ad_id, product_id, period_start, period_end, 花費, 不重複安裝數, 廠商安裝, ... (14 變數) |
| `PerformanceInput`(成效輸入) | **使用者每週貼資料的暫存區**,不在一般同步循環中,由 📥 附加本週成效 按鈕單獨處理 | 廣告代碼, 產品代碼, 期間起, 期間迄, + 14 變數 |
| `DailySpend` | 每日花費(選配,也可由 Ads + AdWeights 即時算) | date, product_id, ad_id, amount_twd |
| `每日攤提覆寫` | 歷史資料覆寫(由舊試算表預估費用主表匯入)。儀表板優先用這個,沒有才算自 Ads | 日期, 產品ID, 每日攤提(台幣), 備註 |
| `Todos` | 確認後待辦 | id, created_at, action_type, payload, status |
| `Settings` | 當月設定 | key, value(含支出匯率、收入匯率、月份起迄等) |
| `Report_by_Product`(衍生) | 給人眼看的,由 Apps Script 依正規化資料產生 | 每個產品一個區塊,列廣告與權重 |

> 為什麼不直接照 `2025採買資料庫.xlsx` 的產品分頁格式:同一筆廣告會出現在多個產品分頁,雙向同步時容易衝突。正規化後增刪改查單純、衝突少;需要舊格式時用 §6.2 的「按產品 CSV 匯出」或 `Report_by_Product` 衍生表補齊。

---

## 8. 技術方向

- **單一靜態網頁**(HTML + JS + CSS,無後端)
- 資料層:瀏覽器本機儲存 + Google Sheets API
- 可直接用瀏覽器開啟使用

---

## 9. 驗證資料來源

- `2025採買資料庫.xlsx`
  - 產品分頁(AV9 / JK / 黃油圈 = HYC / 破解吧 / 汁婦寶 / 萬精游 / 磨欲爽 / 色軟庫 / 熊貓 = 熊貓巴士):全部廣告原始資料
  - 預估費用分頁 **B ~ AK 欄**:4 月每日攤提人工算出的結果(系統產出應該要對得上)
  - 預估費用分頁 **BF ~ CP 欄**:歷次調整後的結果
- `2025_打款_權重紀錄清單.xlsx`
  - 權重紀錄分頁:所有權重變化紀錄(理論上等同上述 BF~CP 欄,可交叉對比)

### 9.1 舊試算表解析規則(匯入舊資料時必看)

**資料列從第 4 列開始**。第 2 列是表頭、第 3 列是彙總或元資料,都要略過。

**欄位對應**(以 1 為起點):
| 欄 | 舊表頭 | 系統欄位 |
|---|---|---|
| C (3) | 編號 | `ad_code` |
| D (4) | 名稱 | `ad_name` |
| E (5) | 產品 | 判斷破圈用(見下) |
| F (6) | 文案 | — |
| G (7) | RMB | `amount_cny` |
| H (8) | 平均一天 | —(系統自己重算) |
| I (9) | 起始 | `start_date` |
| J (10) | 結束 | `end_date` |
| **M (13)** | **廣告天數** | **`amortize_days`(手動值,優先採用)** |
| N (14) | 分類 | `group` |
| P (16) | 分配權重 | 權重值(0~1 的小數,需 × 100 轉成整數 %) |

**破圈拆分規則**:舊試算表分頁名仍然是舊代號(AV9 / JK / 黃油圈 / 熊貓 …),而且把 AV9 + 愛威奶破圈、JK + 健康破圈 **寫在同一個產品分頁裡**。E 欄內容形式是 `AV9-破圈` 或 `JK-破圈`(含連字符),匯入時要拆成兩個獨立產品:

| 舊試算表分頁 | E 欄含「破圈」 | 系統對應產品(代碼 / 名稱) |
|---|---|---|
| AV9 | 否 | `AV9` 愛威奶 |
| AV9 | 是 | `av9_poquan` 愛威奶破圈 |
| JK | 否 | `JK` 健康 |
| JK | 是 | `jk_poquan` 健康破圈 |
| 黃油圈 | — | `HYC` 黃油圈 |
| 破解吧 | — | `PJ8` 破解吧 |
| 汁婦寶 | — | `ZFB` 汁婦寶 |
| 萬精游 | — | `OJI` 萬精游 |
| 磨欲爽 | — | `MYS` 磨欲爽 |
| 色軟庫 | — | `XRK` 色軟庫 |
| 熊貓 | — | `BS` 熊貓巴士 |

拆出來的破圈產品在系統裡是**完全獨立的產品**(自己的月預算、自己的範圍、自己的權重),不和母產品共用任何計算規則。

**合併規則**:用廣告代碼當主索引,合併所有分頁的每日產品貢獻成一條時間線,然後依連續同產品分配(±1 台幣精度)切段。同一分頁內同 (代碼, 產品, 起, 迄) 有多列時,取列號比較大的(最新編輯)。

**參考實作**:
- [samples/allocator.py](samples/allocator.py):用「正確答案」回推分配的程式,產出 `samples/buyads_2026-04.json`(4 月已達 0 誤差)/ `samples/buyads_2026-05.json`
- [samples/ground_truth_2026-04.json](samples/ground_truth_2026-04.json) / [samples/ground_truth_2026-05.json](samples/ground_truth_2026-05.json):使用者手動校正的每日每產品「正確答案」,分配程式以此為分配目標
- [samples/reverse_apr1_from_sheets.py](samples/reverse_apr1_from_sheets.py):獨立驗證 4/1 基準值(PJ8/ZFB/OJI/MYS/XRK/BS 6 個產品 100% 對得上主表)

---

## 10. 已決議規則(歷史問答整理)

- **#3 破圈歸屬**:愛威奶破圈 / 健康破圈 視為**完全獨立的產品**(自己的月預算、自己的範圍、自己的權重),不和愛威奶 / 健康共用任何規則。舊試算表把它們和母產品寫在同一頁,匯入時依 E 欄「破圈」關鍵字 + 4 筆寫錯的硬改修正拆分(§3.4.1、§9.1)。
- **#1 成效目標**:使用者自訂「目標名稱 + 計算公式 + 目標值 + 方向」,公式可以引用 §5.4 列出的 14 個變數。每個產品可以有多個指標。
- **#2 自動調權演算法**:以「有沒有達到該產品的目標值」當好壞依據;單次調整無上限、可以歸零淘汰;唯一硬規定是調整後仍要符合 §3 預算規則。
- **#5 段式紀錄**:所有「續費 / 調權 / 送天數 / 轉移 / 匯率變動」**一律結束舊段、開新段**,用 `renewal_of` 鏈 + `renewal_reason` 欄紀錄原因。不改舊段;歷史已扣金額永不回頭重算(§3.4、§4.2)。
- **#4 APP × 小島混合共購**:可以;但小島那一側仍須嚴格 ±0.5%、APP 那一側 ±30%,任一側違反就算違規(見 §3.2)。
- **#6 匯率變動方式**:未來**不做整批回頭調整**,只在續費當下套用新匯率到新段(§3.4)。舊試算表裡的「匯率調整」整批事件屬歷史特性,分配程式用比例縮放處理但新系統不產生。
- **Google 試算表串接**:用 Apps Script 網頁應用程式(執行身分 = 擁有者、存取 = 任何人),前端用 fetch JSON 動作溝通(§7.2)。
- **試算表結構**:正規化(Products / Ads / AdWeights / …)當單一資料來源;人工瀏覽另走衍生報表(§7.3)。
- **廣告分組 / 分類**:統一叫「**廣告分組**」一個詞(單層),欄位名 `group`,報表依此彙總。
- **跨月攤提**:按實際攤提日切到各月,月預算只佔該月那幾天;匯率沿用鎖定匯率;續費段用續費當下的新匯率(§3.5)。
- **歷史匯入精度**:以使用者提供的 `samples/ground_truth_2026-YY.json` 每日每產品值為終極正確答案;分配程式做到 2026-04 的 330 個 (日 × 產品) 都沒有誤差超過 ±3 台幣。
- **「轉移」與「權重調整」合併**(2026-05 新增):UI 與資料層的「轉移」操作併入「權重調整」,新段一律 `renewal_reason='權重調整'`,跨產品搬遷的細節寫進備註欄;歷史「轉移」段標籤保留為「轉移(舊事件)」(§3.4、§5.7)。
- **影響預覽只算勾選的廣告**(2026-05 新增):權重調整頁的影響預覽只計算「✓ 套用」打勾的廣告,沒勾的維持原權重;手動覆寫但沒打勾不算進預覽(§5.4.1)。
- **跨產品調權**(2026-05 新增):權重調整建議納入該廣告原本 0% 的產品。系統在「某產品少花超過 §3.1 容差」時主動建議加進其他廣告(從多花最嚴重 / 成效最爛的產品挪權重),範圍雙邊檢查(§3.2)不放寬。UI 永遠顯示全部產品的權重欄位(即使 0%)供手動編輯;只要任一產品還有預算可花就顯示視覺提示,門檻 0 與自動建議門檻無關(§5.4.2)。
- **廣告調整建議**(2026-05 重寫,原「送天數空窗處理」):空檔本身不一定要調整。系統實際偵測三類觸發:(a) 任一產品未來日花費 < 建議下限(包含該日完全沒有廣告 baseline = 0 的情況)、(b) APP 月攤提少花 > 6 萬、(c) 未來未採買空檔(僅資訊)。調整演算法按優先序四 pass:① 小島補下限(±0.5% 嚴格)② APP 月預算推到 upper(可破)③ 仍少花 > 6 萬則突破 upper④ APP 補下限(±30%,軟性)。候選來源 = 沒被鎖 + 該日 active 的廣告;0% 產品也可加進去;source 產品優先「多花最嚴重」。雙邊夾擠保證合法,不需事後 LIFO 挪回。彈窗按優先序顯示勾選列,一鍵套用產生「權重調整」段並建 `補日花費缺口` 待辦。「找不到來源」備援文案會主動引導使用者「前往採買建議」並 sessionStorage 帶日期(§5.8)。
- **廣告鎖定狀態三段**(2026-05 新增):每支廣告有 🔓 自由 / 🔒 鎖權重 / 🚫 禁止挪動 三種狀態。鎖定**只影響系統自動建議**,手動編輯永遠放行。🔒 鎖權重 ad 仍可被建議「整桶搬到別產品」(必須是 single-product 100%);🚫 禁止挪動完全不納入自動建議,成效爛只顯示「建議淘汰」。schema 欄位 `lock_perf_adjust` + `lock_full`(§5.7.1)。
- **待辦撤回**(2026-05 新增):每筆會異動廣告資料的 todo 都會帶 `undo_payload`,記錄套用前的廣告快照。在待辦清單按「↩ 撤回」會把資料還原到當時狀態並刪除 todo。沒 payload 的舊 todo 顯示為「刪」,只刪提醒不還原。支援的 action_type:`補日花費缺口`、`成效調整權重`、`手動改權重`、`新增廣告`、`淘汰廣告`(§5.5.1)。
- **action_type 命名(風格 X)**(2026-05 新增):待辦類型用「為什麼要調」當名稱,看名字就知道目的:`補日花費缺口` / `成效調整權重` / `手動改權重` / `新增廣告` / `廣告續費` / `淘汰廣告` / `手動` / `其他`。
- ~~**破圈廣告代碼一律帶 `t` 尾綴**(2026-05 新增)~~ → **已被 2026-05 後續改版覆寫**。新規則:只在「同支廣告 weights 同時含某家族母 + 破圈」(`AV9+av9_poquan` 或 `JK+jk_poquan`)時才拆 t,純破圈 / 跨家族不撞母 / 純破圈多家族共存皆不加 t。家族卡片改靠 weights 內容 + `split_pair_id` 聚合,不靠代碼 t 後綴(§5.3.5、§5.7.2)。
- ~~**事後拆出分流支援雙向**(2026-05 新增)~~ → **按鈕已全部移除**,`buildPoquanSplit` 函式也已從 [app/domain/lifecycle.js](app/domain/lifecycle.js) 移除。改成由「新增廣告」/「權重調整」依 weights 內容**自動偵測同家族碰撞**並拆 pair(§5.7.2);新增函式 `buildWeightAdjustWithAutoSplit` 處理三種 mode:`plain`(無碰撞)/`in_pair`(已配對,走 rebalance)/`split`(觸發拆 t,整支廣告改名)。舊配對的重平衡邏輯 `rebalanceSplitPair` 維持不變。
- ~~**拆破圈/一般分流目標側支援多產品**(2026-05 新增)~~ → **已被自動拆 t 取代**。目標側權重不再手動填,直接由使用者在「權重調整」/「新增廣告」彈窗一次填完全部產品的權重,系統自動把破圈那部分搬到 t-variant、一般留 parent。
- **拆 t 觸發條件 + 改名 lifecycle 事件**(2026-05 新增,**覆寫上述三條**):
  - 觸發條件:同支廣告 weights 同時含某家族母 + 破圈(§5.3.5、§5.7.2)
  - 觸發時整支 ad 改名 `stXXX` ↔ `stXXXt`,所有歷史段一律 follow 新代碼;段層 `code_at_creation` 保留原代碼供時間軸與縮網址歷史對照
  - 新增 `renewal_reason='拆t改名'`(§3.4),寫進拆 pair 觸發當下的新段
  - 權重結構改「跨配對加總 = 100、單支內加總 < 100」(§4.2),不再要求單支內歸一
  - 解除拆 t(一般或破圈那側退場) → **不自動還原**,`stXXXt` 維持不變(改名單向)
  - 鎖定狀態(🔒/🚫)+ 手動編輯觸發拆 t → 跳確認框,確認後正常拆;取消則回滾權重變更
  - 跨家族純破圈共存(例 `st123` av9_poquan 70% + jk_poquan 30%,無 AV9 / JK)→ 不拆 t,儀表板將該支同時計入 AV9 與 JK 家族卡(各依 weight 份額)
- **縮網址管理(per-ad 舊網址 + 全站新網址 cascade)**(2026-05 新增):「🔗 縮網址」頁面採「新網址全站共用、舊網址 per-ad」的混合模型 — 全站只存 `settings.short_url_new_domain`,舊網址寫進每支廣告自己的 `ad.short_url_old_override`(空 = 此廣告沒有舊連結,屬於新合作)。新網址可被 `ad.short_url_new_override` 覆寫,沒寫就 fall back 全站。「💾 套用為新網址」cascade 行為:對 `state.ads` 每支廣告做 snapshot — 該支廣告當下的有效新網址(`new_override` 優先,否則全站舊新值)寫進它自己的 `short_url_old_override`、清掉 `new_override` 讓它繼續跟新全站;`settings.short_url_new_domain ← 輸入值`。這樣**現有廣告**自動帶上歷史,**之後新建的廣告**沒走過 cascade,維持「舊=空」狀態,代表「沒在別處跑過」。複製文字分兩種模板:有舊連結 → 「你好，麻烦广告链结更换\\n[ad_name]\\n文案：  ad_copy\\n旧/新：URL」;無舊連結 → 「新合作 [ad_name]\\n文案：  ad_copy\\n链接：URL」。URL 構造:`https://{採用連結 lowercased}.{domain}/{縮網址參數}`(L1/L3/L5)。覆寫彈窗的舊網域留空 = 強制新合作模板。[app/views/short-urls.js](app/views/short-urls.js)。`settings.short_url_old_domain` 仍在 schema 中但已棄用(cascade 邏輯一執行就清空)。
- **縮網址依站長分組 + 通知狀態追蹤**(2026-05 新增):縮網址頁面依 `contact_info`(站長聯繫資料)把同站長的廣告分組,**多筆同站長**(≥2)會插入群組 header 列(藍底),內含群組通知狀態(✅ 已通知 / ⏳ 未通知)、「📋 通知此站長 (N)」與手動 toggle 按鈕;按下 📋 把整組廣告併在一份模板裡複製(同一句開場 + 每支廣告自己的 block,block 間空一行),並把所有 segments 的 `ad.short_url_notified` 標為 true。空 `contact_info` 或單筆站長的廣告維持原本 per-row 操作(📋 + 狀態 + toggle 在 row 內)。「💾 套用為新網址」cascade 時自動把所有廣告通知狀態重置為 false;另有「↺ 全部標記未通知」按鈕單獨重置(不動網址)。schema 新增 ad 欄位 `縮網址已通知`(Y/空)。[app/views/short-urls.js](app/views/short-urls.js) `groupByContact` / `buildGroupCopyText` / `renderGroupHeader`。
- **同步協定升 v4:Row-level CAS + 衝突 modal**(2026-05 新增):取代原本的 row-level LWW silent overwrite。每筆 row 多 `_version` 欄,push 時帶 `_expected_version`,server CAS 不符就回 `conflicts` 不寫入;pull 時若雙方都改過(本機 fingerprint ≠ sync_meta + server `_updated_at` 更新)也視為衝突。衝突進 [conflict-store](app/io/conflict-store.js) 並彈出 [conflict-resolver](app/io/conflict-resolver.js) modal:per-field diff、三快捷(🟢 全用我的 / 🔵 全用對方 / 🟡 逐欄選擇)+ 全域「全部用我的 / 全部用對方」。「用我的」會把 sync_meta 標 `__force_push__` 並對齊 theirs.version,下次 push CAS 通過;「用對方的」直接套進本機 state 並對齊 sync_meta。衝突未解前 auto-sync 暫停,解完自動恢復。錯誤紀錄走 [sync-log.js](app/lib/sync-log.js),DevTools 打 `__buyadsLog()` 可看最近 200 筆,給「我明明儲存了卻又被改回去」這類抱怨除錯用。Apps Script `Code.gs` 升 v4(`META_COLS` = `_id, _updated_at, _deleted, _version`),首次跑會自動 migrate header(整片清掉 → client resync)。詳見 [§7.2 / §7.2.1](#7-儲存與同步)。

---

## 11. 待釐清問題(實作前再確認)

(目前全部已釐清,實作中若有新問題再補。)
